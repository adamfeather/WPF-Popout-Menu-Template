<UserControl 
    x:Class="PopoutMenuTest.FooterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:fa="http://schemas.fontawesome.io/icons/"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="800">

    <Border Background="{StaticResource ApplicationBlue}">
        <Grid VerticalAlignment="Center">
            
            <WrapPanel VerticalAlignment="Center">
                <fa:ImageAwesome Icon="UserCircleOutline"  Height="14" Margin="6,0,0,0" Foreground="#202020" />

                <TextBlock Margin="5,0,0,0" Foreground="#202020">
                    <Run>Adam Feather</Run>
                    <Run>[Administrator]</Run>
                </TextBlock>
            </WrapPanel>

            <TextBlock HorizontalAlignment="Right" Margin="0,0,16,0" VerticalAlignment="Center" Foreground="#202020">
                <Run>v1.0.0</Run>
            </TextBlock>
        </Grid>
    </Border>
</UserControl>
